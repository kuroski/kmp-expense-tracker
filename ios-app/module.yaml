product: ios/app

dependencies:
    - ../shared

settings:
    compose: enabled
    kotlin:
        freeCompilerArgs:
            - -Xbinary=bundleId=org.expense.tracker.ExpenseTrackerApp
    ios:
        teamId: <your team ID here>
 